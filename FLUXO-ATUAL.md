# recall — Technical Flow

Reference document for implementation.

---

## Tech Stack

| Component | Technology |
|-----------|------------|
| Database | SQLite + sqlite-vec + FTS5 |
| Embeddings | Local via fastembed (`BAAI/bge-small-en-v1.5`, 384 dims, ONNX) |
| Session summary | Generated by Claude itself (no external API) |
| Session content | JSON per session (structured by Claude) |
| Search | Hybrid: FTS5 keyword + sqlite-vec cosine via RRF (Reciprocal Rank Fusion) |
| Configuration | Zero config — no API keys required |

---

## File Structure

```
~/.claude/memory/
  memory.db                              ← SQLite + sqlite-vec + FTS5
  cinemetric_2026-03-29.json             ← session source document
  blueprint_2026-03-28.json
  archived/
    demo-script_2026-03-13.json          ← rotated sessions
```

---

## SQLite Schema

```sql
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,           -- Claude Code session_id
  project_id TEXT,               -- git remote origin url
  cwd TEXT,                      -- project directory
  filename TEXT,                 -- corresponding JSON filename
  title TEXT,                    -- one-line summary
  created_at INTEGER,
  archived INTEGER DEFAULT 0
);

CREATE TABLE chunks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id TEXT,               -- FK → sessions.id
  content TEXT,                  -- chunk text
  chunk_index INTEGER,
  section_type TEXT DEFAULT NULL  -- decisions, tasks_completed, tasks_pending,
                                 -- files_modified, concepts, notes, unknown
);

CREATE VIRTUAL TABLE chunk_embeddings USING vec0(
  chunk_id INTEGER PRIMARY KEY,
  embedding FLOAT[384]           -- BAAI/bge-small-en-v1.5 via fastembed
);

CREATE VIRTUAL TABLE chunks_fts USING fts5(
  content,                       -- FTS5 full-text index
  content=chunks,                -- external content table
  content_rowid=id
);
```

---

## Four Active Points

```
START              EVERY MESSAGE         LONG SESSION          END (manual)
  │                     │                     │                     │
SessionStart    UserPromptSubmit           Stop              /recall-save
  │                     │                     │                     │
List sessions   Auto hybrid search     Save reminder      Claude summary +
+ prompt        + inject context       at 20, 40, 60...   chunk + embed +
/recall-load    via additionalContext   messages            index locally
```

> **SessionEnd does nothing** — the platform does not pass the transcript, making automatic saving impossible. The only real save flow is manual `/recall-save`.

---

## Detailed Flow

### START — Hook: SessionStart

```mermaid
flowchart TD
    A([Claude Code opens]) --> B[Detect project_id:\ngit remote get-url origin]

    B --> C{memory.db exists\nwith sessions for this project?}
    C -->|No| D([Session started — no prior context])
    C -->|Yes| E[List active sessions\nfor this project in SQLite]

    E --> F([Display available sessions\nvia additionalContext\nPrompt to use /recall-load])
```

---

### EVERY MESSAGE — Hook: UserPromptSubmit

**This is the core feature.** Runs on every user message (4+ words), cross-project.

```mermaid
flowchart TD
    A([User sends message]) --> B{Message >= 4 words?}
    B -->|No| C([Skip — too short])
    B -->|Yes| D[Generate embedding locally\nvia fastembed bge-small-en-v1.5]

    D --> E[Hybrid search cross-project\nFTS5 keyword + sqlite-vec cosine]

    E --> F[Reciprocal Rank Fusion\nRRF k=60 merges both rankings]

    F --> G[Recency re-ranking\n80% similarity + 20% exponential decay]

    G --> H{Best blended score\n> 0.75 threshold?}
    H -->|No| I([Exit silently — no injection])
    H -->|Yes| J[Take top 3 chunks\nmax 400 chars each]

    J --> K([Inject via additionalContext\nClaude sees context BEFORE\nprocessing the message])
```

**Key details:**
- Search is always cross-project (`project_id=None`) — finds context from any indexed project
- Query expansion adds domain synonyms PT/EN (e.g., "deploy" → "deploy OR implantação OR deployment")
- FTS5 catches exact keywords, vectors catch semantic equivalents
- Each session gets guaranteed `top_k_per_session=2` slots — large sessions don't drown out smaller ones
- Runs entirely locally (fastembed + SQLite) — sub-100ms latency

---

### LONG SESSION — Hook: Stop

```mermaid
flowchart TD
    A([Claude finishes response]) --> B[Count user messages\nin conversation transcript]

    B --> C{Message count >= threshold?}
    C -->|No| D([Exit silently])
    C -->|Yes| E{Already reminded\nat this threshold?}

    E -->|Yes| D
    E -->|No| F([Suggest /recall-save\nvia systemMessage])

    F --> G[Increment threshold by 20\nnext reminder at current + 20]
```

**Thresholds:** First reminder at 20 messages, then 40, 60, 80... Non-blocking, non-intrusive.

---

### CONTEXT PRESERVATION — Hook: PreCompact

```mermaid
flowchart TD
    A([Context window near limit]) --> B[PreCompact fires]
    B --> C{Any indexed chunks\nfrom previous sessions?}
    C -->|No| D([Returns silently])
    C -->|Yes| E[Generate embedding of current context\nvia fastembed locally]

    E --> F[Hybrid multi-source search\nfor current project_id]
    F --> G[Reinject critical context\nvia systemMessage]

    G --> H([Claude continues with context\npreserved after compaction])
```

---

### END — Command: /recall-save (manual)

The only flow that saves and indexes the session. Must be run before closing.

```mermaid
flowchart TD
    A([User runs /recall-save]) --> B[Claude summarizes\nthe current conversation]
    B --> B2[Structured JSON:\ntitle, decisions, tasks_completed,\ntasks_pending, files_modified,\nconcepts, notes]
    B2 --> C{Session JSON already exists?}
    C -->|Yes| D[Overwrite existing JSON]
    C -->|No| E{Project already has 3 active sessions?}
    E -->|Yes| F[Move oldest to archived/]
    E -->|No| G[Create new JSON]
    F --> G
    D --> H[Semantic chunking by section type\n+ generate embeddings via fastembed\n+ index in sqlite-vec + FTS5]
    G --> H
    H --> I([Session indexed and available\nfor future sessions])
```

---

### Command: /recall-load (manual)

Load specific context on demand.

```mermaid
flowchart TD
    A([User runs /recall-load]) --> B{Has argument?}
    B -->|No| C[List sessions for current project]
    B -->|Number| C2[Load specific session by index]
    B -->|query| D[Hybrid search in current project\nFTS5 + sqlite-vec + RRF]
    B -->|--global query| E[Hybrid search across ALL projects\nblended score threshold >= 0.6]

    C --> F[Display available sessions]
    C2 --> F2[Display full session summary]
    D --> G[Return relevant chunks\nsorted by blended score]
    E --> H[Return cross-project chunks\nwith source project_id]
```

---

## Search Pipeline (5 stages)

Every search — whether from UserPromptSubmit, PreCompact, or /recall-load — goes through the same pipeline:

```
Query → Expansion → Embedding → Hybrid Search → Metadata Filter → Recency Re-rank
```

```mermaid
flowchart LR
    A[Query] --> B[1. Expansion\nDomain synonyms\nPT/EN OR groups]
    B --> C[2. Embedding\nfastembed local\n384 dims]
    C --> D[3. Hybrid Search\nFTS5 keyword +\nsqlite-vec cosine\nmerged via RRF k=60]
    D --> E[4. Metadata Filter\nsection_types\ndays_back]
    E --> F[5. Recency Re-rank\n80% similarity +\n20% exponential decay\nhalf-life 14 days]
```

### Semantic Chunking

Sessions are chunked by logical section, not fixed word count:

| Section | What it captures |
|---------|-----------------|
| `decisions` | Architectural and technical decisions |
| `tasks_completed` | Work done in the session |
| `tasks_pending` | Open work items |
| `concepts` | Key technical concepts discussed |
| `files_modified` | Files changed |
| `notes` | Free-form context |

Each chunk is prefixed with the session title for embedding context.

---

## Full Lifecycle

```mermaid
sequenceDiagram
    participant CC as Claude Code
    participant SH as SessionStart
    participant UPS as UserPromptSubmit
    participant ST as Stop
    participant PC as PreCompact
    participant RS as /recall-save
    participant DB as SQLite + FTS5 + vec

    CC->>SH: Session opens
    SH->>DB: List sessions for project
    DB-->>CC: Prompts /recall-load via additionalContext

    Note over CC: User sends first message

    CC->>UPS: User message (4+ words)
    UPS->>DB: Hybrid cross-project search
    DB-->>CC: Injects relevant chunks via additionalContext

    Note over CC: Work happens... every message triggers UPS

    CC->>UPS: User message N
    UPS->>DB: Hybrid search
    DB-->>CC: Context injection (if score > 0.75)

    CC->>ST: Claude responds (message 20+)
    ST-->>CC: Suggests /recall-save via systemMessage

    CC->>PC: Context window near limit
    PC->>DB: Hybrid search for project
    DB-->>CC: Reinjects critical context

    Note over CC: More work...

    CC->>RS: User runs /recall-save
    RS->>RS: Claude generates structured summary
    RS->>DB: Semantic chunking + fastembed embeddings + FTS5 index
    Note over DB: Session available for all future sessions
```

---

## Summary

| Aspect | Current state |
|--------|---------------|
| Storage | SQLite + sqlite-vec + FTS5 + JSON per session (fallback) |
| Embeddings | Local via fastembed (BAAI/bge-small-en-v1.5, 384 dims, ONNX) |
| Search | Hybrid: FTS5 keyword + sqlite-vec cosine via RRF |
| Query expansion | Domain synonyms PT/EN via OR groups |
| Recency boost | Exponential decay (half-life 14 days), blended 80/20 |
| Semantic chunking | By logical section (decisions, tasks, concepts, notes, files) |
| Cross-project | `project_id=None` — threshold 0.6, sorted by blended score |
| SessionStart | Lists available sessions, prompts `/recall-load` |
| UserPromptSubmit | **Core feature** — auto hybrid search on every message, injects via additionalContext |
| Stop | Save reminder at 20, 40, 60... messages (incremental threshold) |
| PreCompact | Reinjects critical context before compaction |
| SessionEnd | No-op — platform does not pass transcript |
| `/recall-save` | Only save flow — manual, generates summary + indexes locally |
| `/recall-load` | Hybrid search by project or cross-project (`--global`) |
| Session rotation | Max 3 active per project + archived/ |
| API keys required | None — fully local, zero cost |
